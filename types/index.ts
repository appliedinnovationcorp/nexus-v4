/**
 * Main export file for shared types
 */

export * from './common';

// Note: Built-in TypeScript utility types like Partial, Required, etc.
// are globally available and don't need to be re-exported
